# formal-languages-grammar

## Structure

- `backend/`: FastAPI application that handles grammar persistence. See `backend/README.md` for setup instructions.

Frontend scaffolding will be added later in the project timeline.
